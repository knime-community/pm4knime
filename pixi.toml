[workspace]
channels = ["knime", "conda-forge"]
platforms = ["win-64", "linux-64", "osx-64", "osx-arm64"]

[tasks]

[dependencies]
python = "3.*"
knime-extension = "*"
knime-python-base = "5.*"
knime-python-versions = "5.*"
intervaltree = "*"
pydotplus = "*"
pip = "*"

[feature.build.dependencies]
python = "*"
knime-extension-bundling = "5.*"
nodejs = "*"
npm = "*"

[feature.debug.dependencies]
debugpy = "*"

[feature.build.tasks]
build = { args = [{ "arg" = "dest", "default" = "./local-update-site" }], cmd = "build-python-extension . {{ dest }} --update-sites-version latest"}


[environments]
build = {features = ["build"], no-default-feature = true}
debug = {features = ["debug"]}

[pypi-dependencies]
powl = "*"
pm4py = "*"

[pypi-options]
no-build = true